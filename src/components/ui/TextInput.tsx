
import React from 'react'

type Props = {
    placeholder?: string;
    name: string;
}

const Input = (props: Props) => {
    return (
        <input className='' name='' />
    )
}

export default Input