import { IFullUser } from '@/types'
import React from 'react'

type Props = {
    user: IFullUser
}

const PostsTab = (props: Props) => {
    return (
        <div>PostsTab</div>
    )
}

export default PostsTab